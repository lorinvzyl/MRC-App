<?xml version="1.0" encoding="utf-8" ?>
<!--
    This is quite obvious.
-->
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MRC_App.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" RowSpacing="20" Padding="20,0,20,0">
            <!--Define rows needed-->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!--Header-->
            <StackLayout Grid.Row="0" HeightRequest="250" Padding="20" BackgroundColor="{StaticResource Primary}" Margin="-20,0" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <Label Text="&#xf51d;" FontSize="100" TextColor="{StaticResource LightShade}" FontFamily="{StaticResource FontAwesomeSolid}" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
            </StackLayout>
               
            <!--Information block header text-->
            <Label Grid.Row="1" Text="Welcome!" Margin="0,50,0,0" FontSize="24" FontAttributes="Bold" TextColor="Black" HorizontalOptions="CenterAndExpand"/>
            <Label Grid.Row="2" Text="Log into your account" TextColor="Black" HorizontalOptions="CenterAndExpand" Margin="0,-15,0,0"/>
            
            <!--Enter email and password-->
            <Entry Grid.Row="3" FontSize="Medium" Placeholder="Email" HorizontalOptions="FillAndExpand"/>
            <Entry Grid.Row="4" Margin="0,-20,0,0" FontSize="Medium" Placeholder="Password" IsPassword="True" HorizontalOptions="FillAndExpand"/>
            <Label Grid.Row="5" Margin="0,-10,0,0"/>
            <!--Can add a "eye" icon here so they can see the password they entered-->
            
            <!--Button to log into account-->
            <Button Grid.Row="6" Text="Login" BackgroundColor="{StaticResource Primary}" CornerRadius="20"/>
            
            <!--Links for user-->
            <Label Grid.Row="7" Text="Forgot password?" FontAttributes="Bold" HorizontalOptions="CenterAndExpand"/>
            <Label Grid.Row="8" Text="No account? Sign up here" HorizontalOptions="CenterAndExpand"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>